# Backend API URL (required for production)
# Set to your backend's full URL, e.g., https://api.yourdomain.com
# If not set, defaults to /api (useful if backend is proxied on same domain)
VITE_API_URL=https://your-backend-url.com

# Enable/disable public registration (set to "true" to enable)
# When disabled, users can only log in with existing accounts
VITE_REGISTRATION_ENABLED=false
